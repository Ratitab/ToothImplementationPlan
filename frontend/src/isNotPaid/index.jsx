import React from "react";

const IsNotPaid = () => {
  // You can add additional logic here if needed (e.g., log out the user, clean up resources, etc.)

  return (
    <div>
      <h1>Due to Payment</h1>
      <p>Your subscription is not active. Please make the payment to continue using the app.</p>
    </div>
  );
};

export default IsNotPaid;
